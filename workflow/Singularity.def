Bootstrap: docker
From: continuumio/miniconda3:4.12.0

%post
    . /opt/conda/etc/profile.d/conda.sh
    export PATH="/opt/miniconda/bin:$PATH"
    conda create -n gubbins -c defaults -c conda-forge -c bioconda gubbins

%runscript
    . /opt/conda/etc/profile.d/conda.sh
    conda activate gubbins
    echo "Arguments received: $*"
    exec "$@"

%labels
    Author NWaglechner@gmail.com
    Version v0.0.0

%help
    This is based on a demo container used to illustrate a def file